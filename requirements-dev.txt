# Test-only. The suite needs httpx (FastAPI's TestClient is built on it) and
# pytest; neither is a runtime dependency, so they stay out of requirements.txt.
# Without them `pytest` fails on a clean clone with an unhelpful import error.
#
#   pip install -r requirements.txt -r requirements-dev.txt && pytest
pytest>=8.0.0
httpx>=0.27.0
